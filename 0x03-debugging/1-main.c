#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

void main(void)
{
	int i;

	printf("infinite loop coming :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("infinite loop avoided! \\0/\n");

	return (0);
}
